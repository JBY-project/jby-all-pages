# What the preview publishes, one path per line, relative to the repo root.
#
# Not the whole repository: GitHub Pages serves at most 1GB per site and the
# tree here is over 3GB — the home pages carry ~150MB of assets each. So a
# preview names the pages the current work is about, and stays small enough
# that every branch's preview can sit alongside the others.
#
# Blank lines and lines starting with # are ignored.

# This branch is the design-system pass over every page, reviewed on a local
# server. Two pages go up from it, because two are in front of the client:
#
#   the search prototype — the glass, the full-screen phone mode, the header
#   the chat            — Audrey on the launcher, the box on the same glass
#
# They carry 150MB of media each, which is most of the previews site's budget,
# so nothing else from this branch goes up. If the workflow's size guard trips,
# drop whichever of the two has already been signed off.
pages/jby-search
pages/Jeff Brown Yachts - Home Page (Crisp Chat)
